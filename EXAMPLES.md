# 📚 Примеры использования

## Базовые примеры

### Пример 1: Полная обработка (все этапы)
```bash
python subtitle_improver.py video.mp4
```

**Результат в** `outputs/video/run_YYYYMMDD_HHMMSS/`:
- `improved.srt` - английские субтитры
- `subtitled.mp4` - видео с вшитыми субтитрами

Также создается `outputs/video/russian.srt` - русские субтитры

---

### Пример 2: Поэтапная обработка (экономия и гибкость)
```bash
# Шаг 1: Транскрипция через Whisper (один раз, ~$0.06 за 10 мин)
python subtitle_improver.py video.mp4 --step transcribe

# Шаг 2: Перевод через GPT (можно повторять с разными моделями)
python subtitle_improver.py video.mp4 --step translate --model gpt-4o-mini
python subtitle_improver.py video.mp4 --step translate --model gpt-4o

# Шаг 3: Вшивание в видео (использует последний перевод)
python subtitle_improver.py video.mp4 --step burn
```

**Преимущества:**
- Whisper запускается только 1 раз (экономия денег)
- Можно сравнить результаты разных моделей
- Каждый перевод в отдельной папке с timestamp

**Структура после поэтапной обработки:**
```
outputs/
  └── video/
      ├── russian.srt              # Создается при --step transcribe (1 раз!)
      ├── run_20251120_153055/     # Первый --step translate (gpt-4o-mini)
      │   └── improved.srt
      ├── run_20251120_154210/     # Второй --step translate (gpt-4o)
      │   └── improved.srt
      └── run_20251120_155330/     # Третий --step burn
          ├── improved.srt
          └── subtitled.mp4
```

**Ключевой момент:** Русские субтитры (`russian.srt`) создаются только один раз и переиспользуются! При повторном `--step transcribe` программа скажет "уже существуют, пропускаю" → экономия денег! 💰

---

### Пример 3: Максимальное качество (GPT-5)
```bash
python subtitle_improver.py video.mp4 --model gpt-5
```

**Когда использовать:**
- Важные презентации и публичные выступления
- Профессиональный контент для клиентов
- Технические материалы с терминологией

**Стоимость:** ~$0.50-1.00 за 10 минут видео

---

### Пример 3: Только субтитры (без вшивания)
Если хотите отредактировать субтитры вручную перед вшиванием:

```bash
python subtitle_improver.py video1472480268.mp4 --skip-burn
```

**Результат:**
- Только файл `video1472480268_improved.srt`
- Видео не создается

Потом можно вшить вручную:
```bash
ffmpeg -i video1472480268.mp4 -vf subtitles=video1472480268_improved.srt -c:a copy video1472480268_subtitled.mp4
```

---

## Рабочие сценарии

### Сценарий A: Пакетная обработка нескольких видео

**Windows PowerShell:**
```powershell
# Обработать все .mp4 файлы в папке
Get-ChildItem *.mp4 | ForEach-Object {
    python subtitle_improver.py $_.Name
}
```

**Linux/Mac:**
```bash
# Обработать все .mp4 файлы в папке
for video in *.mp4; do
    python subtitle_improver.py "$video"
done
```

---

### Сценарий B: Разные модели для разного контента

**Короткие видео (до 5 мин) - используйте gpt-4o:**
```bash
python subtitle_improver.py short_video.mp4 --model gpt-4o
```

**Длинные видео (10+ мин) - используйте gpt-4o-mini:**
```bash
python subtitle_improver.py long_video.mp4 --model gpt-4o-mini
```

---

### Сценарий C: Создание субтитров для YouTube

1. Создайте только SRT файл:
```bash
python subtitle_improver.py youtube_video.mp4 --skip-burn
```

2. Загрузите видео на YouTube

3. В YouTube Studio загрузите файл `outputs/youtube_video/run_TIMESTAMP/improved.srt` как субтитры

**Преимущества:**
- Зрители могут включать/выключать субтитры
- Субтитры доступны на всех устройствах
- YouTube может автоматически переводить на другие языки

---

### Сценарий D: Сравнение моделей

Транскрибируем один раз, переводим разными моделями:
```bash
# Транскрипция один раз
python subtitle_improver.py video.mp4 --step transcribe

# Тестируем разные модели
python subtitle_improver.py video.mp4 --step translate --model gpt-4o-mini
python subtitle_improver.py video.mp4 --step translate --model gpt-4o
python subtitle_improver.py video.mp4 --step translate --model gpt-5

# Смотрим результаты в папках:
# outputs/video/run_YYYYMMDD_HHMMSS_1/improved.srt
# outputs/video/run_YYYYMMDD_HHMMSS_2/improved.srt
# outputs/video/run_YYYYMMDD_HHMMSS_3/improved.srt

# Вшиваем лучший вариант
python subtitle_improver.py video.mp4 --step burn
```

---

### Сценарий E: Настройка модели по умолчанию

Создайте `.env` файл с предпочитаемыми настройками:

```env
OPENAI_API_KEY=sk-proj-ваш-ключ
GPT_MODEL=gpt-4o
```

Теперь все видео будут использовать gpt-4o по умолчанию:
```bash
python subtitle_improver.py video1.mp4
python subtitle_improver.py video2.mp4
python subtitle_improver.py video3.mp4
```

---

## Продвинутые примеры

### Пример 4: Обработка видео из другой папки

```bash
python subtitle_improver.py "C:\Users\Videos\webinar.mp4"
```

Файлы будут созданы в той же папке где находится исходное видео.

---

### Пример 5: Работа с разными видеоформатами

Скрипт работает с любым форматом, поддерживаемым ffmpeg:

```bash
python subtitle_improver.py video.mp4
python subtitle_improver.py video.avi
python subtitle_improver.py video.mov
python subtitle_improver.py video.mkv
python subtitle_improver.py video.webm
```

---

## Типичные use-cases

### 🎓 Образовательный контент
```bash
# Лекция на русском → субтитры на английском
python subtitle_improver.py lecture_01.mp4 --model gpt-4o-mini
```

### 💼 Бизнес презентации
```bash
# Важная презентация → максимальное качество
python subtitle_improver.py presentation.mp4 --model gpt-4o
```

### 🎬 Видео для YouTube/соцсетей
```bash
# Создать отдельный файл субтитров
python subtitle_improver.py vlog_episode.mp4 --skip-burn
```

### 🎙️ Подкасты и интервью
```bash
# Длинное интервью → оптимальный баланс цена/качество
python subtitle_improver.py interview_2hrs.mp4 --model gpt-4o-mini
```

### 📹 Демо и туториалы
```bash
# Технический контент с вшитыми субтитрами
python subtitle_improver.py tutorial_python.mp4
```

---

## Сравнение результатов

### Пример перевода с gpt-4o-mini:
**Оригинал (RU):** "Привет всем! Сегодня мы будем изучать программирование"
**Перевод:** "Hey everyone! Today we're going to learn programming"

### Пример перевода с gpt-4o:
**Оригинал (RU):** "Привет всем! Сегодня мы будем изучать программирование"
**Перевод:** "Hey everyone! Today we'll be diving into programming"

Оба варианта отличные! GPT-4o-mini обычно более чем достаточно.

---

## Оценка времени и стоимости

### 5-минутное видео
- ⏱️ Время обработки: ~1-1.5 минуты (благодаря async)
- 💰 Whisper: ~$0.03
- 💰 GPT-4o-mini: ~$0.01-0.03 | **Итого: ~$0.04-0.06**
- 💰 GPT-4o: ~$0.05-0.10 | **Итого: ~$0.08-0.13**
- 💰 GPT-5: ~$0.25-0.50 | **Итого: ~$0.28-0.53**

### 10-минутное видео
- ⏱️ Время обработки: ~2-3 минуты
- 💰 Whisper: ~$0.06
- 💰 GPT-4o-mini: ~$0.01-0.05 | **Итого: ~$0.07-0.11**
- 💰 GPT-4o: ~$0.10-0.20 | **Итого: ~$0.16-0.26**
- 💰 GPT-5: ~$0.50-1.00 | **Итого: ~$0.56-1.06**

### 30-минутное видео
- ⏱️ Время обработки: ~6-8 минут
- 💰 Whisper: ~$0.18
- 💰 GPT-4o-mini: ~$0.05-0.15 | **Итого: ~$0.23-0.33**
- 💰 GPT-4o: ~$0.30-0.60 | **Итого: ~$0.48-0.78**
- 💰 GPT-5: ~$1.50-3.00 | **Итого: ~$1.68-3.18**

### 60-минутное видео (1 час)
- ⏱️ Время обработки: ~12-15 минут
- 💰 Whisper: ~$0.36
- 💰 GPT-4o-mini: ~$0.10-0.30 | **Итого: ~$0.46-0.66**
- 💰 GPT-4o: ~$0.60-1.20 | **Итого: ~$0.96-1.56**
- 💰 GPT-5: ~$3.00-6.00 | **Итого: ~$3.36-6.36**

**Примечание:** Время указано для полной обработки (все 3 этапа). Параллельная async обработка ускоряет перевод в 10-12 раз!

---

## Советы по оптимизации

### 💡 Совет 1: Используйте поэтапную обработку
```bash
# Транскрипция один раз
python subtitle_improver.py video.mp4 --step transcribe
# Экспериментируйте с моделями без повторной транскрипции
python subtitle_improver.py video.mp4 --step translate --model gpt-4o-mini
python subtitle_improver.py video.mp4 --step translate --model gpt-4o
```
**Экономия:** Не платите за Whisper повторно!

### 💡 Совет 2: Начните с gpt-4o (по умолчанию)
Для большинства случаев gpt-4o дает отличный баланс качества и цены.

### 💡 Совет 3: Используйте --skip-burn для проверки
```bash
python subtitle_improver.py video.mp4 --skip-burn
# Проверьте outputs/video/run_TIMESTAMP/improved.srt
# Если устраивает - вшейте:
python subtitle_improver.py video.mp4 --step burn
```

### 💡 Совет 4: Batch processing для нескольких видео
```powershell
# Windows PowerShell
Get-ChildItem *.mp4 | ForEach-Object {
    python subtitle_improver.py $_.Name
}
```

### 💡 Совет 5: Версионирование результатов
Все переводы сохраняются! Сравнивайте результаты:
```
outputs/video/run_20251120_153055/improved.srt  # gpt-4o-mini
outputs/video/run_20251120_154210/improved.srt  # gpt-4o
outputs/video/run_20251120_155330/improved.srt  # gpt-5
```

---

---

## 🎨 Как работает умное форматирование

### Проблема: длинные субтитры
Представьте, что GPT перевел одну запись так:
```
"Good afternoon, colleagues! Today we're presenting the design for the phytobar located on the second floor."
```

Это 100+ символов - на экране будет **4 строки одновременно**! 😱 Нечитаемо!

### Решение: автоматическое деление

**Шаг 1:** Разбивка на строки по 45 символов (по словам):
```
Строка 1: "Good afternoon, colleagues! Today we're"
Строка 2: "presenting the design for the phytobar"
Строка 3: "located on the second floor."
```

**Шаг 2:** Если >2 строк → делим на несколько записей:
```
Запись 1 (0-5 сек):
  "Good afternoon, colleagues! Today we're"
  "presenting the design for the phytobar"

Запись 1_1 (5-10 сек):
  "located on the second floor."
```

### Результат: ✅ Читаемые субтитры!
- Максимум 2 строки одновременно на экране
- Весь текст сохранен (ничего не обрезано)
- Время поделено пропорционально
- Связь с оригиналом через суб-индексы

---

**Нужна помощь?** Смотрите:
- `START_HERE.txt` - быстрый старт
- `SETUP_WINDOWS.md` - установка для Windows  
- `README.md` - полная документация
- `CHANGELOG.md` - история изменений

