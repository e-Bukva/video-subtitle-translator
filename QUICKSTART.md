# 🚀 Быстрый старт

## Минимальная инструкция для начала работы

### 1. Установка (один раз)

**Windows - Автоматическая установка:**
```powershell
# Запустите setup.bat (двойной клик):
.\setup.bat
```

**Или вручную:**
```bash
# Установите ffmpeg
# Windows: choco install ffmpeg  (или https://ffmpeg.org/download.html)
# Mac: brew install ffmpeg
# Linux: sudo apt install ffmpeg

# Установите зависимости Python
pip install -r requirements.txt
```

### 2. Настройка API ключа

Получите ключ на https://platform.openai.com/api-keys

**Способ 1 (рекомендуется): Через .env файл**
```powershell
# Скопируйте пример и откройте в блокноте
Copy-Item env.example .env
notepad .env

# Замените your_api_key_here на ваш настоящий ключ
# Сохраните и закройте
```

**Способ 2: Через переменную окружения**
```powershell
# Windows PowerShell:
$env:OPENAI_API_KEY="sk-ваш-ключ-здесь"

# Linux/Mac:
export OPENAI_API_KEY='sk-ваш-ключ-здесь'
```

### 3. Запуск

**Полная обработка (все этапы):**
```bash
python subtitle_improver.py video.mp4
```

**Структура результатов:**
```
outputs/
  └── video/
      ├── russian.srt              # Русские субтитры (создается 1 раз)
      └── run_YYYYMMDD_HHMMSS/     # Каждый запуск в новой папке
          ├── improved.srt          # Английские субтитры
          └── subtitled.mp4         # Видео с субтитрами
```

**Важно:** Русские субтитры создаются только один раз для каждого видео (экономия денег!)

---

## ⚡ Поэтапная обработка (рекомендуется!)

```bash
# Шаг 1: Транскрипция (один раз)
python subtitle_improver.py video.mp4 --step transcribe

# Шаг 2: Перевод (можно повторять с разными моделями!)
python subtitle_improver.py video.mp4 --step translate

# Шаг 3: Вшивание в видео
python subtitle_improver.py video.mp4 --step burn
```

**Преимущества:**
- ✅ Экономия денег (Whisper запускается 1 раз)
- ✅ Эксперименты с моделями без повторной транскрипции
- ✅ Каждый запуск в отдельной папке
- ✅ Умное форматирование: макс 2 строки одновременно, весь текст сохраняется

---

## Дополнительные опции

**Выбор модели:**
```bash
python subtitle_improver.py video.mp4 --model gpt-4o-mini  # Быстрее, дешевле
python subtitle_improver.py video.mp4 --model gpt-4o       # По умолчанию, баланс
python subtitle_improver.py video.mp4 --model gpt-5        # Максимум качества
```

**Только субтитры (без видео):**
```bash
python subtitle_improver.py video.mp4 --skip-burn
```

---

Подробности в [README.md](README.md)

