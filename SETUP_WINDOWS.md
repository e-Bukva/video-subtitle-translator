# 🪟 Инструкция по установке для Windows

Пошаговая инструкция для Windows пользователей.

## Шаг 1: Установка ffmpeg

### Вариант A: Через Chocolatey (рекомендуется)

Если у вас установлен [Chocolatey](https://chocolatey.org/):

```powershell
choco install ffmpeg
```

### Вариант B: Вручную

1. Скачайте ffmpeg: https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip
2. Распакуйте архив (например, в `C:\ffmpeg`)
3. Добавьте `C:\ffmpeg\bin` в PATH:
   - Откройте "Изменение системных переменных среды"
   - Нажмите "Переменные среды"
   - В разделе "Системные переменные" найдите `Path`
   - Нажмите "Изменить" → "Создать"
   - Добавьте путь к папке bin (например: `C:\ffmpeg\bin`)
   - Нажмите "ОК" везде

### Проверка установки

Откройте новое окно PowerShell и выполните:

```powershell
ffmpeg -version
```

Если видите версию ffmpeg - всё установлено правильно! ✅

## Шаг 2: Установка Python зависимостей

Откройте PowerShell в папке проекта и выполните:

```powershell
pip install -r requirements.txt
```

## Шаг 3: Настройка API ключа

### Вариант A: Через .env файл (рекомендуется) ⭐

Это самый удобный способ - ключ сохраняется в файле и работает автоматически.

1. **Получите API ключ:**
   - Зайдите на https://platform.openai.com/api-keys
   - Создайте новый ключ (или скопируйте существующий)
   - Ключ выглядит так: `sk-proj-abc123...`

2. **Создайте .env файл:**

```powershell
# Скопируйте пример
Copy-Item env.example .env

# Откройте .env в блокноте
notepad .env
```

3. **Замените `your_api_key_here` на ваш настоящий ключ:**

```env
OPENAI_API_KEY=sk-proj-ваш_настоящий_ключ_здесь

# Можно также задать модель по умолчанию
GPT_MODEL=gpt-4o-mini
```

4. **Сохраните и закройте блокнот**

Готово! Теперь скрипт автоматически подхватит ключ из `.env` файла. ✅

### Вариант B: Через переменную окружения

Если не хотите использовать .env файл, можно установить переменную для текущей сессии:

**PowerShell:**
```powershell
$env:OPENAI_API_KEY = "sk-proj-ваш-ключ"
```

**CMD:**
```cmd
set OPENAI_API_KEY=sk-proj-ваш-ключ
```

⚠️ **Важно:** Эта переменная будет работать только в текущем окне терминала. После закрытия окна нужно будет установить заново.

## Шаг 4: Запуск скрипта

### Базовое использование (все этапы):

```powershell
python subtitle_improver.py video.mp4
```

Результаты в: `outputs/video/run_YYYYMMDD_HHMMSS/`

---

### ⚡ Поэтапная обработка (рекомендуется!):

```powershell
# Шаг 1: Транскрипция (один раз, ~$0.06 за 10 мин)
python subtitle_improver.py video.mp4 --step transcribe

# Шаг 2: Перевод (можно повторять с разными моделями!)
python subtitle_improver.py video.mp4 --step translate
python subtitle_improver.py video.mp4 --step translate --model gpt-4o-mini

# Шаг 3: Вшивание в видео
python subtitle_improver.py video.mp4 --step burn
```

**Преимущества:**
- Экономия денег (Whisper запускается 1 раз)
- Эксперименты с моделями без повторной транскрипции
- Каждый перевод в отдельной папке

---

### Выбор модели:

```powershell
# GPT-4o-mini (быстрее, дешевле)
python subtitle_improver.py video.mp4 --model gpt-4o-mini

# GPT-4o (по умолчанию, отличный баланс)
python subtitle_improver.py video.mp4 --model gpt-4o

# GPT-5 (максимальное качество)
python subtitle_improver.py video.mp4 --model gpt-5
```

---

### Только субтитры (без вшивания):

```powershell
python subtitle_improver.py video.mp4 --skip-burn
```

---

## 🎯 Результат

Структура папок после обработки:
```
outputs/
  └── video/
      ├── russian.srt              # Русские субтитры (один раз)
      ├── run_20251120_153055/     # Первый запуск
      │   ├── improved.srt         # Английские субтитры
      │   └── subtitled.mp4        # Видео с субтитрами
      └── run_20251120_154210/     # Второй запуск
          └── improved.srt
```

**Как это работает:**
- **Имя папки = имя видеофайла**: `video.mp4` → `outputs/video/`
- **Русские субтитры создаются ОДИН РАЗ**: при повторном запуске пропускаются (экономия!)
- **Каждый перевод в новой папке**: `run_TIMESTAMP` - можно сравнивать модели
- **Ничего не перетирается**: вся история сохраняется

**Преимущества:**
- История всех переводов сохраняется
- Можно сравнивать результаты разных моделей
- Ничего не перетирается
- Русские субтитры переиспользуются (не платим повторно!)
- Умное форматирование: длинные субтитры автоматически делятся (макс 2 строки одновременно)

## 🔧 Решение проблем

### "ffmpeg is not recognized..."

Проблема: ffmpeg не найден в PATH.

**Решение:**
1. Закройте и откройте PowerShell заново (после добавления в PATH)
2. Проверьте что путь к ffmpeg\bin добавлен в PATH
3. Убедитесь что файл `ffmpeg.exe` существует в указанной папке

### "No module named 'openai'"

Проблема: Библиотеки не установлены.

**Решение:**
```powershell
pip install -r requirements.txt
```

### "OPENAI_API_KEY не установлен"

Проблема: Скрипт не видит API ключ.

**Решение:**
1. Проверьте что файл `.env` существует (не `env.example`, а `.env`)
2. Откройте `.env` и убедитесь что ключ вставлен правильно (без пробелов, кавычек)
3. Ключ должен начинаться с `sk-` или `sk-proj-`

### "Unauthorized" или "Invalid API key"

Проблема: API ключ неправильный или истёк.

**Решение:**
1. Проверьте ключ на https://platform.openai.com/api-keys
2. Создайте новый ключ если нужно
3. Убедитесь что у вас есть баланс на счету

### Видео обрабатывается слишком долго

Это нормально! Примерное время для 10-минутного видео:

**Полная обработка (все этапы):**
- Извлечение аудио: ~5-10 секунд
- Whisper транскрипция: ~30-60 секунд
- GPT перевод: ~10-20 секунд (благодаря async!)
- Вшивание субтитров: ~30-60 секунд

**Итого: ~2-3 минуты**

**Поэтапная обработка:**
- `--step transcribe`: ~1-1.5 минуты
- `--step translate`: ~10-20 секунд (⚡ в 10-12 раз быстрее благодаря параллельной обработке!)
- `--step burn`: ~30-60 секунд

## 💡 Советы

1. **Используйте .env файл** - это проще и надёжнее чем переменные окружения
2. **Используйте поэтапную обработку** - экономьте деньги, экспериментируйте с моделями
3. **Начните с gpt-4o** (по умолчанию) - отличный баланс качества и цены
4. **Сравнивайте модели** - транскрибируйте один раз, переводите разными моделями
5. **Храните .env в безопасности** - не делитесь им, не коммитьте в git
6. **Проверьте баланс** на https://platform.openai.com/account/usage перед обработкой длинных видео
7. **История переводов** - все результаты сохраняются в отдельных папках с timestamp

---

**Готово!** Теперь вы готовы обрабатывать видео! 🎉

Если что-то не работает - проверьте каждый шаг по порядку или напишите в issues.

