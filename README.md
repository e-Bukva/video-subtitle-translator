# 🎬 Улучшение субтитров для видео

Python скрипт для создания качественных английских субтитров из русскоязычного видео с использованием OpenAI Whisper и GPT.

## ✨ Возможности

- **Автоматическая транскрипция** русской речи из видео через Whisper API
- **Умный перевод** на естественный английский через GPT (gpt-4o, gpt-4o-mini, gpt-5)
- **⚡ Асинхронная параллельная обработка** - все батчи переводятся одновременно (в 10-12 раз быстрее)
- **🔄 Поэтапное выполнение** - транскрипция, перевод и вшивание как отдельные этапы
- **📁 Версионирование результатов** - каждый запуск в отдельной папке с timestamp
- **🎨 Умное форматирование**:
  - Максимум 2 строки одновременно на экране (читаемость!)
  - Автоматическое деление длинных субтитров с пропорциональным временем
  - Разбивка по 45 символов/строку (по границам слов)
  - **Весь текст сохраняется** - ничего не обрезается!
- **🔁 Автоматические повторы** - непереведенные субтитры обрабатываются повторно (2 уровня)
- **📦 Поддержка больших файлов** - автоматическое разбиение аудио >24MB на части
- **Вшивание субтитров** напрямую в видео (hardcoded subtitles)
- **CLI интерфейс** - простота использования из командной строки

## 📋 Требования

- Python 3.7+
- ffmpeg
- OpenAI API ключ

## 🚀 Установка

### 1. Установите ffmpeg

**Windows:**
```bash
# Скачайте с https://ffmpeg.org/download.html
# Или через chocolatey:
choco install ffmpeg
```

**macOS:**
```bash
brew install ffmpeg
```

**Linux:**
```bash
sudo apt update
sudo apt install ffmpeg
```

### 2. Установите Python зависимости

```bash
pip install -r requirements.txt
```

### 3. Настройте API ключ OpenAI

Получите API ключ на https://platform.openai.com/api-keys

#### Способ A: Через .env файл (рекомендуется) ⭐

Самый удобный способ - создайте файл `.env`:

```bash
# Windows PowerShell:
Copy-Item env.example .env
notepad .env

# Linux/Mac:
cp env.example .env
nano .env
```

В файле `.env` замените `your_api_key_here` на ваш настоящий ключ:

```env
OPENAI_API_KEY=sk-proj-ваш_настоящий_ключ
GPT_MODEL=gpt-4o-mini
```

Готово! Скрипт автоматически подхватит настройки из `.env` файла.

#### Способ B: Через переменную окружения

**Windows (PowerShell):**
```powershell
$env:OPENAI_API_KEY="your-api-key-here"
```

**Windows (CMD):**
```cmd
set OPENAI_API_KEY=your-api-key-here
```

**Linux/macOS:**
```bash
export OPENAI_API_KEY='your-api-key-here'
```

Для постоянного использования добавьте в `.bashrc` / `.zshrc` / `.profile`.

## 💡 Использование

### Базовое использование (все этапы)

```bash
python subtitle_improver.py video.mp4
```

Результат в папке `outputs/video/run_TIMESTAMP/`:
- `improved.srt` - файл с английскими субтитрами
- `subtitled.mp4` - видео с вшитыми субтитрами

### ⚡ Поэтапное выполнение (рекомендуется!)

**Экономьте деньги и время, запуская этапы отдельно:**

```bash
# Шаг 1: Транскрипция (один раз, ~$0.06 за 10 мин видео)
python subtitle_improver.py video.mp4 --step transcribe

# Шаг 2: Перевод (можно повторять с разными моделями!)
python subtitle_improver.py video.mp4 --step translate
python subtitle_improver.py video.mp4 --step translate --model gpt-4o-mini

# Шаг 3: Вшивание в видео (использует последний перевод)
python subtitle_improver.py video.mp4 --step burn
```

**Преимущества:**
- ✅ Whisper запускается только 1 раз (экономия денег)
- ✅ Экспериментируйте с моделями GPT без повторной транскрипции
- ✅ Асинхронная параллельная обработка - перевод в 10-12 раз быстрее
- ✅ Каждый запуск сохраняется в отдельную папку с timestamp
- ✅ Автоматические повторные попытки для непереведенных фрагментов

### Продвинутые опции

**Выбор модели перевода:**
```bash
# GPT-4o (по умолчанию) - отличный баланс качества и скорости
python subtitle_improver.py video.mp4 --model gpt-4o

# GPT-4o-mini - быстрее и дешевле
python subtitle_improver.py video.mp4 --model gpt-4o-mini

# GPT-5 - для максимального качества
python subtitle_improver.py video.mp4 --step translate --model gpt-5
```

Или задайте модель по умолчанию в `.env` файле:
```env
GPT_MODEL=gpt-4o
```

**Создать только .srt файл без вшивания:**
```bash
python subtitle_improver.py video.mp4 --skip-burn
```

**Справка:**
```bash
python subtitle_improver.py --help
```

### 🎨 Как работает умное форматирование?

**Проблема:** GPT может вернуть длинный перевод (100+ символов), который займет 3-4 строки одновременно на экране. Это нечитаемо! 😱

**Решение:** Программа автоматически:
1. Разбивает текст на строки по 45 символов (по границам слов)
2. Если получилось >2 строк → создает несколько субтитров
3. Делит время пропорционально между частями
4. Использует суб-индексы (1, 1_1, 1_2) для связи

**Результат:** Максимум 2 строки одновременно, весь текст сохранен! ✅

**Пример:**
```
До: 1 субтитр, 4 строки, 10 секунд (нечитаемо)
После: 2 субтитра по 2 строки, по 5 секунд (отлично!)
```

### 📘 Подробная инструкция для Windows

Если вы на Windows - смотрите детальную пошаговую инструкцию: [SETUP_WINDOWS.md](SETUP_WINDOWS.md)

## 📊 Процесс работы

### Этап 1: Транскрипция (Whisper)
1. **Извлечение аудио** - ffmpeg извлекает аудиодорожку (128k bitrate)
2. **Разбиение** - если файл >24MB, автоматически делится на части по 10 минут
3. **Транскрипция** - OpenAI Whisper создает русские субтитры с точными таймингами
4. **Сохранение** - русские субтитры сохраняются один раз в `outputs/video/russian.srt`

### Этап 2: Перевод (GPT)
1. **Разбиение на батчи** - субтитры делятся на группы по 40 записей
2. **Параллельная обработка** - все батчи переводятся одновременно (async)
3. **Автоповторы** - непереведенные записи обрабатываются повторно (2 уровня retry)
4. **Умное форматирование** (post-processing):
   - Разбивка текста на строки по 45 символов (без обрезания!)
   - Если >2 строк → деление на несколько SubtitleEntry (макс 2 строки каждая)
   - Время делится пропорционально между частями
   - Суб-индексы (1, 1_1, 1_2) для связи с оригиналом
5. **Сохранение** - результат в `outputs/video/run_TIMESTAMP/improved.srt`

### Этап 3: Вшивание (ffmpeg)
1. **Стилизация** - Arial 20px, белый текст, черная обводка
2. **Кодирование** - видео с hardcoded субтитрами
3. **Сохранение** - результат в `outputs/video/run_TIMESTAMP/subtitled.mp4`

## 🎯 Выбор модели GPT

| Модель | Скорость | Качество | Цена | Рекомендация |
|--------|----------|----------|------|--------------|
| **gpt-4o-mini** | ⚡⚡ Очень быстро | ✅ Хорошо | 💰 Дешево | Для большинства случаев |
| **gpt-4o** | ⚡ Быстро | ⭐ Отлично | 💸 Средне | По умолчанию, отличный баланс |
| **gpt-5** | 🐢 Медленнее | 🌟 Максимум | 💸💸 Дорого | Для критически важного контента |

По умолчанию используется `gpt-4o` - оптимальный баланс качества и скорости для большинства задач.

## 📁 Структура файлов

```
.
├── subtitle_improver.py    # Основной скрипт
├── requirements.txt        # Python зависимости
├── README.md              # Документация
├── CHANGELOG.md           # История изменений
└── video.mp4              # Ваше видео
```

После обработки:
```
.
├── ...
├── video.mp4
└── outputs/
    └── video/
        ├── russian.srt                # Русские субтитры (один раз)
        ├── run_20251120_153055/      # Первый перевод
        │   └── improved.srt
        ├── run_20251120_154210/      # Второй перевод (другая модель)
        │   └── improved.srt
        └── run_20251120_155330/      # С вшиванием видео
            ├── improved.srt
            └── subtitled.mp4
```

**Как работает структура:**

1. **Имя папки = имя видеофайла** 
   - `video.mp4` → `outputs/video/`
   - `presentation.mp4` → `outputs/presentation/`
   - Каждое видео в своей папке!

2. **Русские субтитры создаются ОДИН РАЗ**
   - `outputs/video/russian.srt` - транскрипция через Whisper
   - При повторном запуске: "уже существуют, пропускаю"
   - Экономия денег - не платим за Whisper повторно! 💰

3. **Английские субтитры - каждый раз НОВАЯ папка**
   - `run_20251120_153055/` - первый перевод (gpt-4o-mini)
   - `run_20251120_154210/` - второй перевод (gpt-4o)
   - `run_20251120_155330/` - третий перевод (gpt-5)
   - Можно сравнивать результаты! ✅

4. **Ничего не перезаписывается**
   - Вся история экспериментов сохраняется
   - Можете удалять ненужные папки `run_*` вручную

**Преимущества:**
- История всех переводов сохраняется
- Можно сравнивать результаты разных моделей
- Ничего не перетирается
- Русские субтитры переиспользуются (экономия!)

## ⚙️ Технические детали

### Используемые API

- **Whisper API** (`whisper-1`) - транскрипция аудио с автоматическими таймингами в формате SRT
- **GPT API** (`gpt-4o`, `gpt-4o-mini` или `gpt-5`) - перевод и улучшение текста субтитров
- **AsyncOpenAI** - асинхронная обработка для параллельных запросов

### Асинхронная обработка батчами

Параллельная обработка для максимальной скорости:
- **Размер батча**: 40 записей (оптимально для контекста)
- **Параллельность**: все батчи обрабатываются одновременно
- **Ускорение**: в 10-12 раз быстрее последовательной обработки
- **Автоповторы**: 2 уровня (батчи по 30 и 15) для непереведенных записей

### Умное форматирование субтитров

Автоматическая оптимизация для читаемости:
- **Макс. символов на строку**: 45 (разбивка по границам слов)
- **Макс. строк на экране**: 2 (одновременно)
- **Умное деление**: если перевод длинный (>2 строк) → создается несколько записей
- **Пропорциональное время**: длительность делится равномерно между частями
- **Суб-индексы**: 4 → 4, 4_1, 4_2 (сохранение связи с оригиналом)
- **Без потери текста**: весь контент сохраняется, ничего не обрезается!

**Пример:**
```
Оригинал: 1 запись, 4 строки текста, 10 секунд
Результат: 2 записи по 2 строки, по 5 секунд каждая
```

### Стиль вшитых субтитров

- **Шрифт**: Arial
- **Размер**: 20px
- **Цвет**: белый
- **Обводка**: черная
- **Стиль**: с тенью (BorderStyle=3)
- **Отступ снизу**: 30px (MarginV)

## 🔧 Устранение проблем

**Ошибка "ffmpeg не найден":**
- Убедитесь что ffmpeg установлен и доступен в PATH
- Проверьте: `ffmpeg -version`

**Ошибка "OPENAI_API_KEY не установлен":**
- Убедитесь что переменная окружения установлена
- Проверьте: `echo $OPENAI_API_KEY` (Linux/Mac) или `echo %OPENAI_API_KEY%` (Windows)

**Ошибка API:**
- Проверьте баланс на https://platform.openai.com/account/usage
- Убедитесь что API ключ действителен

**Долгая обработка:**
- Это нормально для длинных видео
- Whisper обрабатывает ~1 минуту аудио за ~5-10 секунд
- GPT перевод занимает ~5-10 секунд на батч

## 💰 Стоимость

Примерная стоимость для 10-минутного видео:

| Компонент | Модель | Стоимость |
|-----------|--------|-----------|
| Транскрипция | Whisper-1 | ~$0.06 |
| Перевод | GPT-4o-mini | ~$0.01-0.05 |
| Перевод | GPT-4o | ~$0.10-0.20 |
| Перевод | GPT-5 | ~$0.50-1.00 |

**Итого для 10 минут:**
- С gpt-4o-mini: ~$0.07-0.11 💰 (самый экономный)
- С gpt-4o: ~$0.16-0.26 💸 (рекомендуется)
- С gpt-5: ~$0.56-1.06 💸💸 (максимальное качество)

## 📝 Примеры

**Пример 1: Полная обработка (все этапы)**
```bash
python subtitle_improver.py presentation.mp4
```

**Пример 2: Поэтапная обработка с экспериментами**
```bash
# Транскрипция один раз
python subtitle_improver.py video.mp4 --step transcribe

# Попробуем разные модели для сравнения
python subtitle_improver.py video.mp4 --step translate --model gpt-4o-mini
python subtitle_improver.py video.mp4 --step translate --model gpt-4o

# Вшиваем лучший вариант
python subtitle_improver.py video.mp4 --step burn
```

**Пример 3: Максимальное качество для важного видео**
```bash
python subtitle_improver.py important_video.mp4 --model gpt-5
```

**Пример 4: Только субтитры без вшивания**
```bash
python subtitle_improver.py video.mp4 --skip-burn
# Результат: outputs/video/run_TIMESTAMP/improved.srt
```

## 🤝 Вклад

Нашли баг? Есть идея? Открывайте issue или pull request!

## 📄 Лицензия

MIT License

## 👨‍💻 Автор

Создано с использованием OpenAI API (Whisper + GPT-4)

---

**Приятного использования!** 🎉

